---
title: Engine Backends
description: Notes on installable SpiderMonkey, JavaScriptCore, and V8 toolchains.
---

## Overview

`o-` can run JavaScript through multiple installable engine toolchains behind
the same `JSEngine` interface.

## SpiderMonkey

Path:

```text
toolchains/spidermonkey
```

Notes:

- Loads the shared `jstd` bootstrap before running user code.
- Uses a dedicated SpiderMonkey runtime wrapper.
- Publishes as an installable binary used by `o- toolchain add`.

## JavaScriptCore

Path:

```text
toolchains/javascriptcore
```

Notes:

- Uses `rust_jsc`.
- Registers shared host callbacks and then evaluates the same `jstd`
  bootstrap used by the other engines.
- Publishes as an installable binary used by `o- toolchain add`.

## V8

Path:

```text
toolchains/v8
```

Notes:

- Uses the Rust `v8` bindings.
- Initializes the V8 platform once, then creates a fresh isolate/context for
  each run.
- Installs the same `__jstd_*` host callbacks and executes the shared
  bootstrap before evaluating your script.
- Publishes as an installable binary used by `o- toolchain add`.

## Shared Contract

All installable toolchains are expected to:

1. implement the shared `JSEngine` trait from `o-core`
2. return the shared `JSError` / `JSResult` types
3. expose the `jstd` host hooks needed by the bootstrap script
4. make common built-ins behave the same way where possible
